#First Problem
SELECT email
FROM Person
GROUP BY email
HAVING COUNT(email) > 1;



#Second Problem
SELECT c.name as Customers
FROM Customers as c
LEFT JOIN Orders as o
ON c.id = o.customerId
WHERE o.id IS NULL;




