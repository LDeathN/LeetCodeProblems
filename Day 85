#First Problem
class Solution(object):
    def canBeTypedWords(self, text, brokenLetters):
        text = text.split(" ")
        result = len(text)

        for word in text:
            for letter in brokenLetters:
                if letter in word:
                    result -= 1
                    break
        
        return result




